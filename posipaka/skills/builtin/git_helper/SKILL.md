---
name: git_helper
description: "Git security tools — secret scanning, history audit, repo hygiene"
triggers: [git, secret, scan, leak, hygiene, commit, секрети, сканування]
category: security
version: "1.0.0"
tags: [git, security, scanning]
---

# Git Helper

Git security tools: scan for leaked secrets, audit commit history for sensitive files,
check repository hygiene (.gitignore, .dockerignore, pre-commit), and perform
safe commits with pre-commit secret scanning.
